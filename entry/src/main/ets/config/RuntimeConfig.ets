/**
 * Default runtime config for the main source set.
 * Source-set overrides:
 * - entry/src/mock/ets/config/RuntimeConfig.ets
 * - entry/src/release/ets/config/RuntimeConfig.ets
 */
export const APP_RUNTIME_ENV: string = 'Other';
export const APP_RUNTIME_SOURCE: string = 'main';
